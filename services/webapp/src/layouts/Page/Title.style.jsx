
import { fonts } from 'app/styles'

const getStyles = () => ({
    wrapper: {
        ...fonts.title,
        marginLeft: 10,
        marginRight: 10,
        marginTop: 15,
        marginBottom: 10,
    },
})

export default getStyles
