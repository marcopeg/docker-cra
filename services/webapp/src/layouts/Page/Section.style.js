
import { fonts } from 'app/styles'

const getStyles = () => ({
    wrapper: {
        ...fonts.text,
        marginLeft: 10,
        marginRight: 10,
        marginTop: 15,
        marginBottom: 15,
    },
})

export default getStyles
