
import { fonts } from 'styles'

const getStyles = () => ({
    wrapper: {
        ...fonts.text,
        marginLeft: 50,
        marginRight: 50,
        marginBottom: 30,
    },
})

export default getStyles
